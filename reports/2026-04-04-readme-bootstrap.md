# README Bootstrap (2026-04-04)

## Summary

Created a top-level `README.md` for repository onboarding.

## Added Content

- Project overview and purpose
- Current implementation status
- Core API concepts (`Compare`, result model, slot model)
- Container behavior (Dictionary/List/IEnumerable)
- Strict mode behavior and exception types
- String key policy (`Ordinal` / `OrdinalIgnoreCase` and canonical `KeyText`)
- Quick start commands (`dotnet build`, `dotnet test`)
- Minimal usage example with `[CompareKey]`
- Pointers to `doc/`, `tasks/`, and `reports/`
- License reference

## Files

- `README.md` (new)
