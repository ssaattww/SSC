# Feedback Points

- Updated: 2026-04-03

## 2026-04-03

- ユーザー指摘: `tasks` フォルダ等を適宜作成すること。
- 対応:
  - `tasks/tasks-status.md` を新規作成
  - `tasks/phases-status.md` を新規作成
  - `tasks/feedback-points.md` を新規作成
- ユーザー指摘: こまめに commit すること。
- 対応:
  - 以後、作業区切りごとに小さくコミットする運用へ変更
- ユーザー指摘: 開発方法で指摘された場合、メモを残すこと。
- 対応:
  - 開発方法に関する指摘は必ず本ファイルに追記する運用を固定
- ユーザー指摘: Git 操作時の確認を最小化したい。
- 対応:
  - 権限昇格付きの `git add` / `git commit -m` を継続許可前提で運用
- ユーザー指摘: Dictionary 等の他コンテナ対応と `SelectMany` の動作仕様を明確化したい。
- 対応:
  - 詳細設計にコンテナ対応方針（章17）と `SelectMany` 仕様（章18）を追加
- ユーザー指摘: Git 権限確認はできるだけ広く許可したい。
- 対応:
  - Windows 環境の許可方式上、実際にはコマンド全文一致に近い許可が保存される点を共有
  - 以後は同型コマンドを固定して確認回数を最小化
- ユーザー指摘: コンテナ対応と `SelectMany` は例示付きで記載してほしい。
- 対応:
  - 設計書にコンテナ別の例示（章17.6）と `SelectMany` 例示（章18.5）を追記
